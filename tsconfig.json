import { FeatureCard } from "@/components/FeatureCard";
import { Footer } from "@/components/Footer";
import { HeroSplit } from "@/components/HeroSplit";
import { Section } from "@/components/Section";
import { SiteNav } from "@/components/SiteNav";

export default function HomePage() {
  return (
    <main>
      <SiteNav />
      <HeroSplit />
      <Section>
        <div className="grid gap-4 md:grid-cols-3">
          <FeatureCard title="Fast onboarding" body="Create your profile and start a guided program in minutes." />
          <FeatureCard title="Hands-on modules" body="Learn by doing with practical assignments and feedback loops." />
          <FeatureCard title="Cohort analytics" body="Track engagement, completion, and learner outcomes." />
        </div>
      </Section>
      <Footer />
    </main>
  );
}
